apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: portfolio
  labels:
    app: ai-service
    app.kubernetes.io/name: ai-service
    app.kubernetes.io/component: ai
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
