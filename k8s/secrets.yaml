apiVersion: v1
kind: Secret
metadata:
  name: portfolio-secrets
  namespace: portfolio
  labels:
    app.kubernetes.io/name: portfolio
type: Opaque
stringData:
  # ===========================================
  # IMPORTANT: Replace these placeholder values
  # with your actual secrets before applying!
  # ===========================================
  
  # Database (Neon PostgreSQL)
  # Get this from your Neon dashboard
  # Removed channel_binding=require as it can cause issues with container networking
  DATABASE_URL: "postgresql://neondb_owner:npg_7RxSoelpv4Jt@ep-spring-band-anpix9ru-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require"
  
  # JWT Secret for authentication
  # Generate with: openssl rand -base64 32
  JWT_SECRET: "your-super-secret-jwt-key-change-this"
  
  # OpenAI API Key
  # Get this from https://platform.openai.com/api-keys
  OPENAI_API_KEY: ""
  
  # Admin credentials (optional, for seeding)
  ADMIN_EMAIL: "admin@prajwalraj.me"
  ADMIN_PASSWORD: "change-this-password"
